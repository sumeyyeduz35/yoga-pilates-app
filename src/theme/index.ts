import tokens from './tokens.json';

export const theme = tokens;
export type Theme = typeof theme;

export { disciplineTheme } from './discipline-theme';
export type { DisciplineKey } from './discipline-theme';
